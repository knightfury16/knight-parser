Hello im {{name}}
{{#if boy }}
	My favourite food is {{#if isCool }}
    {{ coolFood }} name
    {{#else}}
    normal food name {{#endif}}
{{#endif}}
{{#for people}}
	{{#if this.isEmployed}}
    	{{this.name}}
        {{#else}}
        	unemployed
     {{#endif}}
    {{this}}
{{#endfor}}
